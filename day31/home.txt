Создайте такой метод

метод принимает параметр n
создает n потоков каждый из которых должен запуститься с методом

def waiter(n):
    print(f'thread n started')
    sleep(3)
    print(f'thread n ended')


слодный вариант

первый метод создает n потоков
в каждом созданном потоке должно создаться 4 потока выполняющих метод waiter

