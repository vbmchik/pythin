1 сделать график - вводим в поле строку выражения с переменной подумайте 
как ее правильно там передавать точнее когда считаем значения вместо переменной ставить текущее значение икс
sin(@x) [1,2,3,4,5,6,7] [sin(1),sin(2),sin(3),...]           str.replace('@x', )

2 ввод данных в базу из формы


все в СВОЙ git